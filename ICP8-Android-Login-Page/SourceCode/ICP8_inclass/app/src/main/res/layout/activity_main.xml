<?xml version="1.0" encoding="utf-8"?>
<!--Set XML header-->
<!--Define layout for our activity features-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="MainActivity"
    android:background="@drawable/login_bg">

    <!--A header text item to specify this as sign in field-->
    <TextView
        android:id="@+id/SignIn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="20dp"
        android:gravity="center"
        android:padding="20dp"
        android:text="Sign in"
        android:textColor="@color/white"
        android:textSize="30sp" />

    <!--Input space for user to enter username-->
    <EditText
        android:id="@+id/username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/SignIn"
        android:layout_marginStart="20dp"
        android:inputType="textPersonName"
        android:imeOptions="actionNext"
        android:minHeight="48dp"
        android:hint="Username"
        android:textColorHint="@color/white"
        android:textColor="@color/white"/>

    <!--Input space for user to enter password-->
    <EditText
        android:id="@+id/password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/username"
        android:layout_marginStart="20dp"
        android:inputType="textPassword"
        android:imeOptions="actionNext"
        android:minHeight="48dp"
        android:hint="Enter Password"
        android:textColorHint="@color/white"
        android:textColor="@color/white"/>

    <!--Set the design for the button that signs a user in-->
    <Button
        android:id="@+id/btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="20dp"
        android:text="Login"
        android:layout_below="@id/password"
        android:backgroundTint="@color/black"
        android:imeOptions="actionNext"/>
</RelativeLayout>